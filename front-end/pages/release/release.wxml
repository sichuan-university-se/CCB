<!--pages/release/release.wxml-->

<view class='margin bg-white radius shadow-lg padding-top'>
  <view class='text-center margin-bottom text-lg  text-grey'>{{title[type]}}</view>
  <view class="cu-form-group solid-bottom">
    <view class="title">标题</view>
    <input placeholder="请输入标题"></input>
  </view>
  <view class="cu-form-group solid-bottom">
    <textarea maxlength="-1" disabled="{{modalName!=null}}" bindinput="textareaInput" placeholder="请输入详细描述"></textarea>
  </view>
  <view class="cu-form-group solid-bottom">
    <view class="title">要求验证</view>
    <switch class="orange radius sm" checked></switch>
  </view>
  <view class="cu-bar">
    <view class="action">
      图片上传
    </view>
    <view class="action">
      {{imgList.length}}/4
    </view>
  </view>
  <view class="cu-form-group">
    <view class="grid col-4 grid-square flex-sub">
      <view class="bg-img" wx:for="{{imgList}}" wx:key="{{index}}" bindtap="ViewImage" data-url="{{imgList[index]}}">
        <image src='{{imgList[index]}}' mode='aspectFill'></image>
        <view class="cu-tag bg-red" catchtap="DelImg" data-index="{{index}}">
          <text class="cuIcon-close"></text>
        </view>
      </view>
      <view class="solids" bindtap="ChooseImage" wx:if="{{imgList.length<4}}">
        <text class="cuIcon-cameraadd"></text>
      </view>
    </view>
  </view>
</view>